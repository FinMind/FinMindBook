version: '3.0'
services:
  crawler_twse:
    image: crawler3:7.2.1
    hostname: "twse"
    command: uv run --env-file=.env celery -A financialdata.tasks.worker worker --loglevel=info --concurrency=1  --hostname=%h -Q twse
    restart: always
    environment:
      - TZ=Asia/Taipei
    networks:
        - my_network

  crawler_tpex:
    image: crawler3:7.2.1
    hostname: "tpex"
    command: uv run --env-file=.env celery -A financialdata.tasks.worker worker --loglevel=info --concurrency=1  --hostname=%h -Q tpex
    restart: always
    environment:
      - TZ=Asia/Taipei
    networks:
        - my_network

  crawler_taifex:
    image: crawler3:7.2.1
    hostname: "taifex"
    command: uv run --env-file=.env celery -A financialdata.tasks.worker worker --loglevel=info --concurrency=1  --hostname=%h -Q taifex
    restart: always
    environment:
      - TZ=Asia/Taipei
    networks:
        - my_network

networks:
  my_network:
    # 加入已經存在的網路
    external: true
